(defun ranger-find-functions ()
  (interactive)
  (let ((inhibit-message t))
    (ranger-find-file "~/.emacs.d/lisp/functions/")))

(defun ranger-find-org-dir ()
  (interactive)
  (let ((inhibit-message t))
    (ranger-find-file "~/org")))

(defun ranger-find-pcc-dir ()
  (interactive)
  (let ((inhibit-message t))
    (ranger-find-file "~/PCC")))

(defun ranger-find-planning ()
  (interactive)
  (let ((inhibit-message t))
    (ranger-find-file "~/org/Planning")))

(defun ranger-find-materias ()
  (interactive)
  (let ((inhibit-message t))
    (ranger-find-file "~/Matérias")))

(defun ranger-find-config-dir ()
  (interactive)
  (let ((inhibit-message t))
    (ranger-find-file "~/.config")))

(defun ranger-find-bashdot ()
  (interactive)
  (let ((inhibit-message t))
    (ranger-find-file "/home/dotfiles/bash/bashdot")))

(defun ranger-find-nvim-dir ()
  (interactive)
  (let ((inhibit-message t))
    (ranger-find-file "~/.config/nvim")))

(defun ranger-find-links-dir ()
  (interactive)
  (let ((inhibit-message t))
    (ranger-find-file "~/links")))

(defun ranger-find-scripts-dir ()
  (interactive)
  (let ((inhibit-message t))
    (ranger-find-file "~/scripts")))

(defun ranger-find-emacs-dir ()
  (interactive)
  (let ((inhibit-message t))
    (ranger-find-file "~/.emacs.d")))

(defun ranger-find-org-dir ()
  (interactive)
  (let ((inhibit-message t))
    (ranger-find-file "~/org")))

(defun find-links-dir ()
  (interactive)
  (let ((inhibit-message t))
    (ranger-find-file "~/links")))
